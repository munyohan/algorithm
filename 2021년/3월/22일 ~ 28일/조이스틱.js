'use strict';

function solution(name) {
  let answer = 0;

  return answer;
}

// 모든 글자가 전부 A에서 시작.
// 첫 글자부터 시작해서 A에서 갈 수 있는 최소 이동 수를 계산
// 한 칸 이동시 더하기
// 각 글자마다 첫 칸 처럼 계산해서 더하기
// 마지막 글자까지 더하면 return해주기

console.log(solution('JAN'));
